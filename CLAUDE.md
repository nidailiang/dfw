# DFW — A 股个股数据分析

## 这是什么

A 股个股数据分析仓库,分三层:

- **第 1 层**:行情 + 技术指标(纯机器计算,无人工判断)
- **第 2 层**:财务基本面(机器采集 + AI 解读)
- **第 3 层**:资金面(数据源受限,暂缓)

## 分析一只股票的标准流程

用户说"分析 `<代码> <中文名>`"(如"分析 688072 拓荆科技")时,按以下流程执行。
也可以让用户直接跑一条龙:`python analyze_one.py <代码> <中文名>`。

1. **第 1 层 · 技术面**
   - `python chart_html.py <代码> <中文名>` → 交互式 K 线图(stocks/中文名/中文名_k线.html)
   - `python analyze_mc.py <代码> <中文名>` → 蒙特卡洛价格区间(看 P5 参考止损区)
2. **第 2 层 · 基本面**(顺序固定:先采集后解读)
   - `python collect_data.py <代码> <中文名>` → 采集 10 类数据(基本面.json)
   - `python analyze_fundamental.py <代码> <中文名>` → AI 解读 7 章报告(分析报告.md)
3. **收尾**:把人工判断、关键价位写进 `stocks/<中文名>/分析笔记.md`(判断不进脚本)

## 约定

- 分析产物一律放 `stocks/<中文名>/`,目录和文件用股票**中文名**,不用代码(用户认中文名)
- 脚本只做机器计算和采集,个股判断写分析笔记,不写死进脚本
- 图表参数:白线(多空线)= EMA(EMA(C,10),10),黄线 = (MA14+MA28+MA57+MA114)/4,写死;均线组参数可配
- AI 调用通过环境变量 `ANTHROPIC_BASE_URL` / `ANTHROPIC_AUTH_TOKEN` 注入,**不硬编码 token**(会随 git 泄露)
- 分析脚本按主题参数化(通用脚本),不按股票复制;个别专项脚本暂留根目录待通用化
- 行情数据源用新浪日K线(东财 push2 在此环境被断);行业 PE 接口"列不匹配"是参数问题可修
- `1.指标公式/` `2.选股公式/` `selection/` `technical/` 是通达信公式文本,与代码无关;`setup/` 是 Claude Code 安装配置,与股票无关
- 持仓/交易相关问题:用户是个人 A 股交易者,做理性拆解伙伴,不替用户做买卖决定

## 常用脚本

| 脚本 | 用途 |
|---|---|
| `analyze_one.py` | 一条龙:图表 + 蒙特卡洛 + 采集 + AI 报告 |
| `chart_html.py` | 交互式 K 线图(四段:价格/量/KDJ/MACD) |
| `analyze_mc.py` | 蒙特卡洛价格区间模拟 |
| `b1_check.py` | B1 止损检查(买入价 − k×ATR) |
| `collect_data.py` / `analyze_fundamental.py` | 第 2 层采集 / AI 解读 |
| `fetch_stock_kline.py` | 数据基础:日K线 + 指标计算 |
| `check_holdings.py` | 持仓检查 |
