{M1 14; M2 28; M3 57; M4 114}
短期:EMA(EMA(C,10),10);
多空:(MA(CLOSE,14)+MA(CLOSE,28)+MA(CLOSE,57)+MA(CLOSE,114))/4;
昨短:=REF(短期,1);
昨多:=REF(多空,1);
NOTEXT_基准短:CONST(昨短),COLORYELLOW,DOTLINE;
NOTEXT_基准多:CONST(昨多),COLORYELLOW,DOTLINE;
MA2:=EMA(CLOSE,13);
Z1:=STRCAT(HYBLOCK,' ');
Z2:=STRCAT(Z1,DYBLOCK);
Z3:=STRCAT(Z2,' ');
DRAWTEXT_FIX(ISLASTBAR,0,0,0,STRCAT(Z3,GNBLOCK)),COLOR00C0C0;
MA5:EMA(CLOSE,5),COLORGREEN,NODRAW;
额:AMOUNT/100000000,NODRAW,COLORRED;
{ 波段折线（指南针风格）}
{ 参数：N 为反转幅度百分比，默认5%}
N:=5;
ZIGZAG: ZIG(3,N), COLORFF00FF, DOTLINE,LINETHICK1;

{ 标注高低点位置（可选）}
{DRAWICON(ZIGZIG>REF(ZIGZIG,1) AND ZIGZIG>REFX(ZIGZIG,1), ZIGZIG, 1); { 高点画笑脸 }
{DRAWICON(ZIGZIG<REF(ZIGZIG,1) AND ZIGZIG<REFX(ZIGZIG,1), ZIGZIG, 2); { 低点画哭脸 }

{ 显示当前折线值 }
{DRAWTEXT(ISLASTBAR, ZIGZIG, ' 波段折线'), COLORWHITE;

{ 注：ZIG函数存在未来函数，回测时需注意，实际看图无影响 };